PROCEDURE FALSE;
BEGIN
	(* Недопустимі оператори та знаки *)
	A = B + C - D * E / F;

	(* Спеціальні символи та пунктуація *)
        VAL ? & # @ ! ~ ^;

(* Неправильні шляхи або системні знаки *)
PATH \ FILE [ ] { } < > .

(*символ долара($ $)*)
$
END;